import 'package:flutter/material.dart';

class InitialStateDisplay extends StatelessWidget {
  const InitialStateDisplay({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: SizedBox());
  }
}
